# 评分链路上下文工程计划

## 当前结论

该计划已达到可交付 MVP 级别。

## 目标

为 `evaluate_submission` 建立服务端持久化的上下文工程能力，解决当前一次性 prompt 拼装带来的不稳定、不可复现和难审计问题。

## 已落地范围

- 新增 `evaluation_context_snapshots`，保存脱敏上下文包、文本摘要、版本和 `input_hash`
- 评分前先生成上下文快照，再构造 prompt 调用模型
- `llm_call_logs` 关联 `context_snapshot_id`
- 提供教师/管理员查看最新快照和历史快照接口

## 当前默认决策

- 只覆盖评分链路，不做通用聊天会话系统
- 只保存脱敏后的上下文，不保存未脱敏原文
- 不引入向量库、embedding、rerank 或通用 RAG
- PostgreSQL 继续作为唯一持久化存储

## 仍待后续推进

- 对教师/学生多轮对话场景扩展为独立会话层
- 对超长证据做更细粒度 chunk/retrieve，而不是当前结构化预算裁剪
- 在前端增加上下文快照只读查看界面
- 把远端 Git 仓库更深度内容探测纳入上下文构建输入
